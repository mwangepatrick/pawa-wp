# Workspace Notes

This workspace contains the `pawa-wp-mcp` repo at `pawa-wp-mcp/`.
Claude Code should load the workspace MCP configuration from `.mcp.json` at the workspace root.
The workspace settings file at `.claude/settings.json` enables project MCP servers automatically.
