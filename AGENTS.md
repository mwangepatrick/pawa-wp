# Workspace Instructions

This workspace contains the `pawa-wp-mcp/` repo and related site content under `content/` and `plugins/`.
Use the `pawa-wp-mcp` MCP server for WordPress site management tasks in this workspace.
Prefer the existing workspace docs and repo README for project-specific conventions.
